export const reservations = [];

// blockedSlots ahora guarda un objeto con el ID del juego y la cantidad de unidades bloqueadas
// Ejemplo: { 'YYYY-MM-DD': { 'HH:MM': { gameId: count, gameId: count } } }
export const blockedSlots = {};